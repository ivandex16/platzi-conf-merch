import styled from 'styled-components'
import {motion} from 'framer-motion'

export const Container = styled(motion.main)`
margin: 0 0 4em 0;

`

export const SuccessMap = styled(motion.section)`
margin: 10px 0 0 0;
`